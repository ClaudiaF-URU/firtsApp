import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { IfPage } from './if';

@NgModule({
  declarations: [
    IfPage,
  ],
  imports: [
    IonicPageModule.forChild(IfPage),
  ],
})
export class IfPageModule {}
